namespace MyProperty.Application.Admin.Queries.GetStakeholderDashboard;

/// <summary>
/// Loads the system-wide stakeholder (product-lead) dashboard. Takes no
/// parameters — the data is global, not scoped to the calling user.
/// </summary>
public sealed record GetStakeholderDashboardQuery();
