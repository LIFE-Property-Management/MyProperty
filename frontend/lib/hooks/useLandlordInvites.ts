// Landlord – Invites hooks
// ENDPOINTS.landlordInvites             → GET  /landlord/invites
// ENDPOINTS.landlordInvites             → POST /landlord/invites  (body: email, propertyId, lease terms)
// ENDPOINTS.landlordResendInvite(id)    → POST /landlord/invites/{id}/resend
// ENDPOINTS.landlordCancelInvite(id)    → POST /landlord/invites/{id}/cancel
