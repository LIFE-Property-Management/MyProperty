// Landlord – Tenants hooks
// ENDPOINTS.landlordTenants             → GET /landlord/tenants
// ENDPOINTS.landlordTenantById(id)      → GET /landlord/tenants/{id}
// ENDPOINTS.landlordTenantPayments(id)  → GET /landlord/tenants/{id}/payments (query: status, page, pageSize)
// ENDPOINTS.landlordConfirmPayment(id)  → POST /landlord/payments/{id}/confirm
// ENDPOINTS.landlordRejectPayment(id)   → POST /landlord/payments/{id}/reject
