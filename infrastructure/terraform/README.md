# MyProperty infrastructure — Terraform

Provisions DigitalOcean resources for the MyProperty production-shaped deployment:
DOKS cluster, managed Postgres, Spaces buckets.

Full operations spec: [`docs/operations/terraform.md`](../../docs/operations/terraform.md).

Bootstrap (one-time state bucket setup): see [`bootstrap/README.md`](./bootstrap/README.md).
